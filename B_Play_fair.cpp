#include <bits/stdc++.h>
using namespace std;
#define int long long int

int32_t main()
{
    int t = 1;
    // cin>>t;
    while (t--)
    {
        int n, k, x, count = 0;
        cin >> n;
        // vector<int> vec(n);
        // for (int i = 0; i < n; i++)
        // {
        //     cin >> vec[i];
        // }
        cout << n * 2;
    }
    return 0;
}